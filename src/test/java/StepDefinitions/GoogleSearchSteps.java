package StepDefinitions;

import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;
import io.grpc.Context.Key;

public class GoogleSearchSteps {
	/*
	 * WebDriver driver;
	 * 
	 * @Given("browser is open") public void browser_is_open() {
	 * System.out.println("browser open ");
	 * System.setProperty("webdriver.chrome.driver",
	 * "C:\\Users\\Shanj\\eclipse-workspace\\project_CC\\src\\test\\resources\\driver\\chromedriver.exe"
	 * ); driver = new ChromeDriver(); driver.manage().timeouts().implicitlyWait(30,
	 * TimeUnit.SECONDS); driver.manage().timeouts().pageLoadTimeout(30,
	 * TimeUnit.SECONDS);
	 * 
	 * 
	 * //driver.manage().window().maximize();
	 * 
	 * }
	 * 
	 * @And("user is on google search page") public void
	 * user_is_on_google_search_page() { System.out.println("google search steps");
	 * driver.navigate().to("https://twitter.com/login");
	 * 
	 * }
	 * 
	 * @When("user enters a test in search box") public void
	 * user_enters_a_test_in_search_box() {
	 * System.out.println(" text in search box ");
	 * driver.findElement(By.name("q")).sendKeys("cromacampus"); }
	 * 
	 * @And("hits enter") public void hits_enter() {
	 * System.out.println("hit enters steps");
	 * driver.findElement(By.name("q")).sendKeys(Keys.ENTER); }
	 * 
	 * @Then("user is navigated to search result") public void
	 * user_is_navigated_to_search_result() {
	 * System.out.println("navigate search result"); driver.getPageSource().
	 * contains("Croma Campus: Best IT Training Institute in Noida");
	 * //driver.close(); }
	 */
}
